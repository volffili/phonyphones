import style from './Footer.module.css';

const Footer = () => (
    <footer className={style.footer}>
        <cite>When nothing is done, nothing is left undone.</cite>(Tao Te Ching chapter 48)
    </footer>
)

export default Footer;